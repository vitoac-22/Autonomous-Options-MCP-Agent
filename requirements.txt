# Direct dependencies only. pip resolves the transitive tree.
#
# This was a 238-package `pip freeze` of a developer laptop, which broke CI:
# audioop-lts (an audio library, imported nowhere) requires Python >= 3.13, so
# `pip install -r requirements.txt` failed outright on 3.12. Any one of 238
# unrelated pins can do that again. These are the packages the code imports.

# Broker, market data, and the official MCP server
alpaca-py==0.44.0
requests==2.34.2

# Model Context Protocol — our own server plus the stdio client
mcp==2.1.1
mcp-types==2.1.1

# LLM client (Featherless is OpenAI-compatible)
openai>=1.50.0

# Quantitative core
arch==8.0.0              # GARCH(1,1) skewed-t
cvxpy==1.9.2             # convex sizing
numpy==2.5.1
pandas==3.0.3
scipy==1.18.0
statsmodels==0.14.6

# Runtime
python-dotenv==1.2.3
pytz==2026.2

# Dashboard
streamlit==1.59.2
